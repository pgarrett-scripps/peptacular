sequence module
===============

The `sequence` module includes various submodules such as `sequence` (self), `combinatoric`, and `mod_builder`. Each submodule offers different functionalities within the `sequence` context.

.. toctree::
   :maxdepth: 2

   sequence_submodule
   combinatoric_submodule
   mod_builder_submodule